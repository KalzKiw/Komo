// This module no longer holds static config.
// Cutoff schedule is loaded from the `settings` table via settings.service.ts.
// Import getOrderSchedule() from services/settings.service wherever needed.
export {};
